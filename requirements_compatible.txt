# Amulet-AI - Python 3.13 Compatible Requirements
# ระบบที่ทำงานได้จริงกับ Python 3.13

# Core web framework
streamlit>=1.28.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# API and HTTP
requests>=2.31.0
python-multipart>=0.0.6

# Image processing (ทำงานได้กับ Python 3.13)
Pillow>=10.0.0
opencv-python>=4.8.0

# Data processing (ทำงานได้)
numpy>=1.24.0
pandas>=2.0.0

# Machine Learning - Hybrid approach
scikit-learn>=1.3.0
scipy>=1.11.0
joblib>=1.3.0

# Deep Learning - CPU versions for feature extraction
torch>=2.0.0+cpu
torchvision>=0.15.0+cpu
# Install with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Visualization - Alternative to matplotlib
plotly>=5.15.0
seaborn>=0.12.0  # อาจมีปัญหา เพราะต้องใช้ matplotlib

# Performance monitoring
psutil>=5.9.0
tqdm>=4.65.0

# Development and utilities
python-dotenv>=1.0.0
pydantic>=2.4.0
typing-extensions>=4.8.0

# Async support
aiofiles>=23.2.1

# Alternative packages for Python 3.13
# แทน matplotlib
bokeh>=3.3.0
# แทน faiss
# (ใช้ sklearn's NearestNeighbors แทน)

# Optional: TensorFlow Lite สำหรับ model inference
# tensorflow-lite>=2.13.0

# ไม่รวม packages ที่มีปัญหา:
# tensorflow (full)  - มีปัญหา _pywrap_tf2  
# faiss-cpu         - มีปัญหา circular import
# matplotlib        - มีปัญหา kiwisolver._cext (ใช้ plotly แทน)