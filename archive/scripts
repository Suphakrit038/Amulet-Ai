#!/usr/bin/env python3
"""
🧹 Cleanup Script - ลบไฟล์ที่ซ้ำซ้อนและไม่จำเป็น
"""

import os
import sys

def main():
    # ไฟล์ที่ต้องลบ (ซ้ำซ้อนกับสิ่งที่อยู่ใน scripts/ folder)
    files_to_delete = [
        # Launcher scripts (ซ้ำกับ scripts/main_launcher.py)
        'launch_complete_system.py',
        'launch_system.py', 
        'simple_launcher.py',
        'start.py',
        'system_launcher.py',
        'quick_start.py',
        'setup_complete_system.py',
        'start_amulet_system.py',
        'launch_real_ai_system.py',
        
        # Test scripts (ซ้ำกับ scripts/test_system.py)
        'test_api_connection.py',
        'test_api_import.py', 
        'test_real_ai_system.py',
        'test_simple.py',
        'test_system.py',
        'quick_test.py',
        'final_test.py',
        
        # Fix/Emergency scripts (ไม่ใช้แล้ว)
        'emergency_fix.py',
        'fix_and_run_system.py',
        'run_streamlit_fast.py',
        
        # Batch files (ซ้ำกับ scripts/start_system.bat)  
        'launch.bat',
        'fix_streamlit.bat',
        'launch_real_ai_backend.bat',
        'launch_real_ai_complete.bat', 
        'run_streamlit_fast.bat',
        'start_amulet_system.bat',
        'start_system.bat',
        
        # Duplicate files
        'README_NEW.md',
        'requirements_fixed.txt'
    ]
    
    print("🧹 กำลังลบไฟล์ที่ซ้ำซ้อน...")
    deleted_count = 0
    
    for file in files_to_delete:
        if os.path.exists(file):
            try:
                os.remove(file)
                print(f"✅ ลบ: {file}")
                deleted_count += 1
            except Exception as e:
                print(f"❌ ไม่สามารถลบ {file}: {e}")
        else:
            print(f"⚠️  ไม่พบไฟล์: {file}")
    
    print(f"\n🎉 ลบไฟล์แล้ว {deleted_count} ไฟล์")
    print("📁 ไฟล์หลักที่เหลือ:")
    print("   - scripts/main_launcher.py (เริ่มระบบ)")
    print("   - scripts/test_system.py (ทดสอบ)") 
    print("   - scripts/start_system.bat (Windows)")

if __name__ == "__main__":
    main()
